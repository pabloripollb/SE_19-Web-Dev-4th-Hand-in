<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Admin Login</title>
  <link rel="stylesheet" href="/styles/index.css">
  <style>
    /* Estilos simples para el login */
    .login-container {
      max-width: 500px; margin: 100px auto; padding: 40px;
      background: var(--color-background-dark); color: var(--color-text-light);
      border-radius: 12px;
    }
    .login-container h1 { color: var(--color-accent); }
    .login-container form { display: flex; flex-direction: column; gap: 20px; }
    .login-container input { padding: 10px; font-size: 1rem; }
  </style>
</head>
<body style="background: #E8E2D2;">
  <div class="login-container">
    <h1>Panel de Admin</h1>
    <form action="/admin/login" method="POST">
      <label for="password">Contraseña:</label>
      <input type="password" id="password" name="password" required>
      <button type="submit" class="cta-button">Entrar</button>
    </form>
    <% if (error) { %>
      <p style="color: red; margin-top: 15px;"><%= error %></p>
    <% } %>
  </div>
</body>
</html>